[package]
name = "warder-journal"
version = "1.0.0-beta.1"
edition.workspace = true
license.workspace = true

[dependencies]
aya = { version = "0.13.1", optional = true }
bytes = { version = "1", optional = true }
libc = "0.2"

[features]
live-ebpf = ["dep:aya", "dep:bytes"]
