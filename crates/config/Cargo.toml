[package]
name = "warder-config"
version = "1.0.0-beta.1"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
toml = "0.8"
warder-core = { path = "../core" }
