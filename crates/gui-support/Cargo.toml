[package]
name = "warder-gui-support"
version = "1.0.0-beta.1"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
warder-config = { path = "../config" }
