[package]
name = "warder-snapshot"
version = "1.0.0-beta.1"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
warder-core = { path = "../core" }
