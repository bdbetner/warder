{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Main Warder desktop window. Filesystem access, process launch, receipts, and journals are mediated by validated Rust commands rather than broad Tauri plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
