session: session-demo
agent: Local Shell (local-shell)
profile: local-script
status: completed
exit code: 0
command: sh -c 'echo hello > /tmp/warder-quickstart/protected/hello.txt'
protected zones: quickstart-protected
root pid: 12345
cgroup: degraded: cgroup tagging skipped because --cgroup-root was not provided
landlock: not requested
snapshot: not requested
session readiness: degraded
blocked reasons: none
degraded reasons:
- cgroup tagging skipped because --cgroup-root was not provided
receipt limitations:
- Warder only supervises commands launched through warder run or the Warder desktop launcher; commands run directly outside Warder are not contained.
- Protected-path reads are not blocked by default; only explicit read_policy = "deny" zones with valid readable_roots use experimental Landlock read blocking.
- limited to inotify protected-path changes plus live eBPF observations of common path syscalls, fd writes, ftruncate(2), writable mmap(2)/mprotect(2), sendfile(2), splice(2), and copy_file_range(2) when attached; fd and mmap observations may not resolve back to protected paths and remain visibility-only, not enforcement
- limited to observed TCP connect(2), UDP sendto(2)/sendmsg(2)/sendmmsg(2), socket send(2), and socket-like sendfile(2)/splice(2) fd activity when live eBPF network journaling is attached, plus connected socket snapshots from procfs during supervised runs; not complete socket forensics or enforcement
- Network policy is visibility-only in this alpha; allowed destinations are not enforced and quiet network journals are not proof of no egress.
- Receipts and the local SQLite journal are accountability records with a local hash chain; they are not tamper-proof forensics against a process that can modify Warder's state.
dependency changes: none_detected (command metadata does not reference known package managers or dependency files)
dependency file changes: none
file activity: 3 event(s)
file activity zones: quickstart-protected=3
file activity sources: inotify=3
file activity attribution: session-window=3
network activity: 0 event(s)
degraded coverage: 1 reason(s)
coverage degraded reasons:
- cgroup tagging skipped because --cgroup-root was not provided
review guidance:
- Inspect file activity rollups and raw journal events for unexpected protected-zone changes.
- Inotify session-window events are observational and are not PID-attributed enforcement evidence.
- Treat degraded coverage as incomplete protection, not success.
recovery guidance:
- Run `warder journal --db /tmp/warder-quickstart/warder.sqlite3 --session session-demo --file` to inspect protected-zone file activity.
- Rerun after addressing degraded coverage if this session needed strong protection.
- Run `warder doctor` to inspect host readiness before rerunning a session that needed strong protection.
